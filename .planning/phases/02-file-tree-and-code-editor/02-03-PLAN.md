---
phase: 02-file-tree-and-code-editor
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - nano_claude/widgets/__init__.py
  - nano_claude/widgets/search_overlay.py
  - nano_claude/widgets/searchable_text_area.py
  - nano_claude/panels/editor.py
  - nano_claude/services/__init__.py
  - nano_claude/services/file_watcher.py
  - nano_claude/panels/file_tree.py
  - nano_claude/app.py
  - tests/test_search.py
  - tests/test_file_watcher.py
autonomous: true
requirements: [EDIT-06, TREE-04]

must_haves:
  truths:
    - "User presses Ctrl+F and a search bar appears at the top of the editor panel"
    - "User types a query and all matches are highlighted simultaneously in the file, with the current match in a distinct brighter color"
    - "User presses Enter or Ctrl+G to jump to the next match, Shift+Enter or Ctrl+Shift+G for previous"
    - "User presses Escape and the search bar closes and all highlights are cleared"
    - "When files are added or removed externally, the file tree updates automatically without collapsing expanded directories"
  artifacts:
    - path: "nano_claude/widgets/search_overlay.py"
      provides: "SearchOverlay widget with Input for search queries and match count display"
      contains: "class SearchOverlay"
    - path: "nano_claude/widgets/searchable_text_area.py"
      provides: "SearchableTextArea subclass with multi-match highlighting support"
      contains: "class SearchableTextArea"
    - path: "nano_claude/services/file_watcher.py"
      provides: "FileWatcherService using watchfiles awatch() for filesystem monitoring"
      contains: "class FileWatcherService"
    - path: "tests/test_search.py"
      provides: "Tests for search overlay and match finding"
    - path: "tests/test_file_watcher.py"
      provides: "Tests for filesystem watcher integration"
  key_links:
    - from: "nano_claude/panels/editor.py"
      to: "nano_claude/widgets/search_overlay.py"
      via: "composes SearchOverlay and handles SearchRequested messages"
      pattern: "SearchOverlay"
    - from: "nano_claude/panels/editor.py"
      to: "nano_claude/widgets/searchable_text_area.py"
      via: "uses SearchableTextArea instead of plain TextArea for multi-match highlighting"
      pattern: "SearchableTextArea"
    - from: "nano_claude/app.py"
      to: "nano_claude/services/file_watcher.py"
      via: "starts watcher on mount, handles FileSystemChanged, stops on unmount"
      pattern: "FileWatcherService"
    - from: "nano_claude/app.py"
      to: "nano_claude/panels/file_tree.py"
      via: "on_file_system_changed handler calls file_tree.reload_preserving_state()"
      pattern: "on_file_system_changed"
---

<objective>
Add in-editor search (Ctrl+F overlay with find/navigate matches and simultaneous all-match highlighting) and filesystem watcher for automatic tree refresh when files change externally.

Purpose: EDIT-06 requires users to search within the current file with all matches highlighted simultaneously (per user decision). TREE-04 requires the tree to auto-refresh when files are added/removed externally (e.g., git checkout, another tool). The filesystem watcher is also foundational for Phase 4 (Change Detection).
Output: Search overlay widget, SearchableTextArea with multi-match highlighting, file watcher service, and integration with editor and tree panels.
</objective>

<execution_context>
@/Users/yaroslavpankovych/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yaroslavpankovych/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-tree-and-code-editor/02-CONTEXT.md
@.planning/phases/02-file-tree-and-code-editor/02-RESEARCH.md
@.planning/phases/02-file-tree-and-code-editor/02-01-SUMMARY.md
@.planning/phases/02-file-tree-and-code-editor/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and Plan 02 outputs. -->

From nano_claude/panels/editor.py (after Plan 02):
```python
class EditorPanel(BasePanel):
    current_file: reactive[Path | None] = reactive(None)
    _buffer_manager: BufferManager
    _text_area: TextArea  # reference stored on_mount

    def compose(self):
        self.panel_title = "Editor"
        yield TextArea.code_editor("", language=None, theme="monokai", soft_wrap=False, id="code-editor")

    def open_file(self, path: Path) -> None: ...
    def save_current_file(self) -> None: ...
    def has_unsaved_changes(self) -> bool: ...
    def _update_title(self) -> None: ...
```

From nano_claude/panels/file_tree.py (after Plan 01):
```python
class FilteredDirectoryTree(DirectoryTree):
    show_hidden = reactive(False)
    def filter_paths(self, paths: Iterable[Path]) -> Iterable[Path]: ...

class FileTreePanel(BasePanel):
    def compose(self):
        self.panel_title = "Files"
        yield FilteredDirectoryTree(Path.cwd(), id="directory-tree")
```

From nano_claude/app.py (after Plan 02):
```python
class NanoClaudeApp(App):
    BINDINGS = [
        # ... existing bindings ...
        Binding("ctrl+s", "save_file", "Save", id="file.save", priority=True, show=True),
        Binding("ctrl+h", "toggle_hidden_files", ..., id="tree.toggle_hidden", ...),
    ]
    def on_directory_tree_file_selected(self, event): ...
    def action_save_file(self): ...
    def action_quit(self): ...
```

From textual (verified API):
```python
TextArea.document -> Document
Document.get_line(line_index: int) -> str
Document.line_count -> int
TextArea.selection -> Selection  # get/set selection range
TextArea.cursor_location -> tuple[int, int]
TextArea.scroll_cursor_visible() -> None
Selection(start=(row, col), end=(row, col))
# TextArea._render_line_impl is the internal method that renders each visible line.
# TextArea uses Rich Text/Style internally for line rendering.
# TextArea.render_line(y: int) -> Strip is the public render method.
```

From watchfiles (verified API):
```python
from watchfiles import awatch, DefaultFilter, Change
# Change enum: added, modified, deleted
# awatch(path, watch_filter=..., debounce=800, stop_event=anyio.Event()) -> AsyncIterator[set[tuple[Change, str]]]
# DefaultFilter(ignore_dirs=(...))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SearchableTextArea, search overlay widget, and integrate with editor panel</name>
  <files>nano_claude/widgets/__init__.py, nano_claude/widgets/search_overlay.py, nano_claude/widgets/searchable_text_area.py, nano_claude/panels/editor.py, nano_claude/app.py, tests/test_search.py</files>
  <read_first>
    - nano_claude/panels/editor.py
    - nano_claude/app.py
    - nano_claude/config/settings.py
  </read_first>
  <behavior>
    - Test: find_all_matches("hello", "hello world\nhello again\nbye") returns [(0, 0), (1, 0)]
    - Test: find_all_matches("HELLO", "hello world") returns [(0, 0)] (case-insensitive)
    - Test: find_all_matches("", "any text") returns []
    - Test: find_all_matches("no", "yes") returns []
    - Test: SearchOverlay is hidden by default (display: none)
    - Test: SearchOverlay becomes visible when toggled
    - Test: EditorPanel has action_toggle_search method
    - Test: App has Ctrl+F binding for search
    - Test: SearchableTextArea can store match_positions and current_match_index
    - Test: SearchableTextArea.set_search_matches stores positions and triggers refresh
    - Test: SearchableTextArea.clear_search_matches resets state
  </behavior>
  <action>
    1. Create nano_claude/widgets/__init__.py with imports for SearchOverlay and SearchableTextArea.

    2. Create nano_claude/widgets/searchable_text_area.py:
       - Import TextArea from textual.widgets, Style from rich.style, Selection from textual.widgets.text_area
       - Create class SearchableTextArea(TextArea):
         - Purpose: Subclass that supports highlighting ALL search matches simultaneously (per user decision: "All matches highlighted in the file simultaneously, current match in a different/brighter color").
         - _match_positions: list[tuple[int, int]] = []  (row, col pairs)
         - _match_query_len: int = 0
         - _current_match_index: int = -1
         - _MATCH_STYLE: Rich Style for non-current matches -- use Style(bgcolor="grey30") or similar muted background
         - _CURRENT_MATCH_STYLE: Rich Style for current match -- use Style(bgcolor="yellow", color="black") for bright/distinct color
         - set_search_matches(matches: list[tuple[int, int]], query_len: int) -> None:
           - Store _match_positions = matches, _match_query_len = query_len, _current_match_index = 0 if matches else -1
           - Call self.refresh() to trigger re-render with highlights
         - set_current_match(index: int) -> None:
           - self._current_match_index = index
           - If index >= 0 and index < len(_match_positions): move selection to current match via Selection, call scroll_cursor_visible()
           - Call self.refresh()
         - clear_search_matches() -> None:
           - self._match_positions = [], _match_query_len = 0, _current_match_index = -1
           - Call self.refresh()
         - Override render_line(y: int) -> Strip:
           - Call strip = super().render_line(y) to get the default rendered line
           - If _match_positions is empty, return strip unchanged
           - Determine which document row corresponds to this visual line y:
             - Use self.scroll_offset to compute: doc_row = y + self.scroll_offset.y
             - (Account for line numbers gutter offset if present -- the Strip from super() includes gutter)
           - Find all matches on this doc_row from _match_positions
           - For each match on this row, apply style overlay to the Strip:
             - Convert (row, col) to visual column offset (accounting for gutter/line number width)
             - Use strip.apply_style(style, start_col, end_col) or manually create a new Strip with styled segments
             - For the current match (_current_match_index), use _CURRENT_MATCH_STYLE
             - For all other matches, use _MATCH_STYLE
           - Return the modified strip
         - IMPORTANT IMPLEMENTATION NOTE: TextArea.render_line() returns a Textual Strip object. To apply match highlights, use Strip.crop() and Strip.join() or iterate over Strip._segments to inject Rich styles at the correct column offsets. If render_line override proves too fragile (the Strip structure varies with gutter, line numbers, cursor), use this alternative approach:
           - Instead of overriding render_line, override _render_line_impl if it exists, OR
           - Use post_render patching: override render_line, call super(), then apply_meta or style_strip to inject background colors at match positions
           - If ALL override approaches fail during execution, fall back to: current match via Selection (bright) + non-current matches displayed only as a count in the search bar ("3 of 12"). Document this as a known limitation in the SUMMARY if the fallback is used.
       - Add a class method for creating a searchable code editor:
         - @classmethod code_editor(cls, ...) -> "SearchableTextArea": delegates to TextArea.code_editor() pattern but returns SearchableTextArea instance. Alternatively, use SearchableTextArea(text, language=..., theme="monokai", show_line_numbers=True, soft_wrap=False, tab_behavior="indent", id="code-editor") directly in EditorPanel.compose().

    3. Create nano_claude/widgets/search_overlay.py:
       - Import Horizontal from textual.containers, Input and Static from textual.widgets, Message from textual.message
       - Create class SearchOverlay(Horizontal):
         - class SearchRequested(Message):
           - def __init__(self, query: str, direction: int = 1): super().__init__(); self.query = query; self.direction = direction
           - direction: 1 = forward, -1 = backward
         - class SearchClosed(Message): pass
         - DEFAULT_CSS:
           ```
           SearchOverlay {
               dock: top;
               height: 3;
               display: none;
               background: $surface;
               border-bottom: solid $primary;
               padding: 0 1;
           }
           SearchOverlay.visible {
               display: block;
           }
           SearchOverlay Input {
               width: 1fr;
           }
           SearchOverlay #search-count {
               width: auto;
               min-width: 8;
               content-align: center middle;
               padding: 0 1;
           }
           ```
         - compose(): yield Input(placeholder="Find...", id="search-input"); yield Static("0/0", id="search-count")
         - on_input_submitted(event: Input.Submitted): post_message(SearchRequested(event.value, direction=1))
         - on_key(event: Key):
           - If event.key == "escape": toggle off, post SearchClosed
           - If event.key == "ctrl+g": post SearchRequested with direction=1 (next match)
           - If event.key == "ctrl+shift+g" or "shift+enter": post SearchRequested with direction=-1 (prev match)
         - show_overlay(): add_class("visible"); query_one("#search-input", Input).focus()
         - hide_overlay(): remove_class("visible"); query_one("#search-input", Input).value = ""
         - update_count(current: int, total: int): query_one("#search-count", Static).update(f"{current}/{total}")

    4. Update nano_claude/panels/editor.py:
       - Import SearchOverlay from nano_claude.widgets.search_overlay
       - Import SearchableTextArea from nano_claude.widgets.searchable_text_area
       - Change compose() to yield SearchableTextArea instead of TextArea.code_editor():
         - yield SearchOverlay(id="search-overlay") BEFORE the SearchableTextArea (so it docks at top)
         - yield SearchableTextArea("", language=None, theme="monokai", show_line_numbers=True, soft_wrap=False, tab_behavior="indent", id="code-editor")
       - Update on_mount() to store self._text_area = self.query_one("#code-editor", SearchableTextArea)
       - Add _search_matches: list[tuple[int, int]] = [] (stores (row, col) of each match)
       - Add _current_match_index: int = -1
       - Add _last_search_query: str = ""
       - Add find_all_matches(query: str) -> list[tuple[int, int]]:
         - If not query, return []
         - Case-insensitive search (query.lower(), line.lower())
         - Iterate over all lines: self._text_area.document.get_line(row) for row in range(self._text_area.document.line_count)
         - Find all occurrences of query in each line, record (row, col)
         - Return list of (row, col) tuples
       - Add action_toggle_search():
         - Get overlay = self.query_one(SearchOverlay)
         - If overlay.has_class("visible"): hide overlay, clear matches via self._text_area.clear_search_matches(), clear _search_matches, reset _current_match_index and _last_search_query
         - Else: show overlay
       - Add on_search_overlay_search_requested(event: SearchOverlay.SearchRequested):
         - If this is a new query (differs from _last_search_query):
           - _search_matches = find_all_matches(event.query)
           - _last_search_query = event.query
           - _current_match_index = 0 if matches else -1
           - self._text_area.set_search_matches(_search_matches, len(event.query))
         - Else (same query, navigating):
           - If direction is 1 (forward): increment _current_match_index (wrapping: (_current_match_index + 1) % len(_search_matches))
           - If direction is -1 (backward): decrement _current_match_index (wrapping)
         - Update overlay count display: overlay.update_count(_current_match_index + 1, len(_search_matches))
         - If matches exist: self._text_area.set_current_match(_current_match_index) which highlights current match brightly and scrolls to it
       - Add on_search_overlay_search_closed(event):
         - self._text_area.clear_search_matches()
         - Clear _search_matches, reset _current_match_index, _last_search_query
         - Focus back on SearchableTextArea
    5. Add to nano_claude/app.py BINDINGS:
       - Binding("ctrl+f", "toggle_search", "Find", id="editor.find", priority=True, show=True)
       - Add action_toggle_search(): query EditorPanel, call editor.action_toggle_search()
    6. Write tests in tests/test_search.py:
       - test_find_all_matches_basic: direct call to find_all_matches helper with known string
       - test_find_all_matches_case_insensitive: uppercase query matches lowercase text
       - test_find_all_matches_empty_query: returns empty list
       - test_find_all_matches_no_match: returns empty list
       - test_search_overlay_hidden_by_default: mount app, verify SearchOverlay does not have "visible" class
       - test_ctrl_f_binding_exists: verify binding in app BINDINGS
       - test_searchable_text_area_stores_matches: create SearchableTextArea, call set_search_matches, verify _match_positions stored
       - test_searchable_text_area_clear_matches: call clear_search_matches, verify _match_positions is empty
  </action>
  <verify>
    <automated>cd /Users/yaroslavpankovych/nano-claude && .venv/bin/python -m pytest tests/test_search.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class SearchOverlay" nano_claude/widgets/search_overlay.py
    - grep -q "class SearchRequested" nano_claude/widgets/search_overlay.py
    - grep -q "class SearchableTextArea" nano_claude/widgets/searchable_text_area.py
    - grep -q "set_search_matches" nano_claude/widgets/searchable_text_area.py
    - grep -q "render_line" nano_claude/widgets/searchable_text_area.py
    - grep -q "_MATCH_STYLE" nano_claude/widgets/searchable_text_area.py
    - grep -q "_CURRENT_MATCH_STYLE" nano_claude/widgets/searchable_text_area.py
    - grep -q "find_all_matches" nano_claude/panels/editor.py
    - grep -q "action_toggle_search" nano_claude/panels/editor.py
    - grep -q "SearchableTextArea" nano_claude/panels/editor.py
    - grep -q "ctrl+f" nano_claude/app.py
    - grep -q "editor.find" nano_claude/app.py
    - grep -q "SearchOverlay" nano_claude/panels/editor.py
  </acceptance_criteria>
  <done>SearchOverlay docks at top of editor, hidden by default. Ctrl+F toggles it. Typing query finds all case-insensitive matches. All matches highlighted simultaneously via SearchableTextArea render_line override (muted background for non-current, bright yellow for current match -- per user decision). Enter/Ctrl+G jumps to next match. Shift+Enter/Ctrl+Shift+G jumps to previous. Escape closes overlay and clears all highlights. Match count shown as "N/M". All search tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create file watcher service and wire to tree auto-refresh</name>
  <files>nano_claude/services/__init__.py, nano_claude/services/file_watcher.py, nano_claude/panels/file_tree.py, nano_claude/app.py, tests/test_file_watcher.py</files>
  <read_first>
    - nano_claude/panels/file_tree.py
    - nano_claude/app.py
    - nano_claude/config/settings.py
  </read_first>
  <behavior>
    - Test: FileWatcherService can be instantiated with a path
    - Test: FileSystemChanged message contains a set of changes
    - Test: FileTreePanel.reload_preserving_state captures expanded directories before reload
    - Test: App starts the watcher on mount and stops on unmount
    - Test: App.on_file_system_changed calls file_tree.reload_preserving_state()
  </behavior>
  <action>
    1. Create nano_claude/services/__init__.py (empty or with import).
    2. Create nano_claude/services/file_watcher.py:
       - Import Path, Message from textual.message, anyio
       - Import awatch, DefaultFilter, Change from watchfiles
       - Create class FileSystemChanged(Message):
         - def __init__(self, changes: set[tuple[Change, str]]): super().__init__(); self.changes = changes
       - Create class FileWatcherService:
         - def __init__(self, app, watch_path: Path):
           - self.app = app
           - self.watch_path = watch_path
           - self._stop_event: anyio.Event | None = None
         - async def start(self) -> None:
           - self._stop_event = anyio.Event()
           - Create DefaultFilter with ignore_dirs=("__pycache__", ".git", "node_modules", ".venv", ".mypy_cache", ".pytest_cache", ".ruff_cache")
           - async for changes in awatch(self.watch_path, watch_filter=filter, debounce=800, stop_event=self._stop_event):
             - self.app.post_message(FileSystemChanged(changes))
         - def stop(self) -> None:
           - if self._stop_event: self._stop_event.set()
    3. Update nano_claude/panels/file_tree.py:
       - Add async method reload_preserving_state(self):
         - Get tree = self.query_one(FilteredDirectoryTree)
         - Capture expanded paths: walk tree nodes recursively, collect path for each node where node.is_expanded is True
         - await tree.reload()
         - After reload, walk nodes again and re-expand any whose path was in the saved expanded set
       - Do NOT add an on_file_system_changed handler to FileTreePanel. The app.py handles this event and calls reload_preserving_state() on the panel (consistent with Phase 1's app-level coordination pattern where the app orchestrates inter-panel communication).
    4. Update nano_claude/app.py:
       - Import FileWatcherService from nano_claude.services.file_watcher
       - Import FileSystemChanged from nano_claude.services.file_watcher
       - In on_mount: create self._file_watcher = FileWatcherService(self, Path.cwd()); self.run_worker(self._file_watcher.start(), exclusive=True, name="file-watcher")
       - Add on_file_system_changed(self, event: FileSystemChanged):
         - This is the SOLE handler for FileSystemChanged -- app.py owns this coordination (not FileTreePanel).
         - Get file_tree = self.query_one(FileTreePanel)
         - Schedule the async reload: self.call_later(file_tree.reload_preserving_state) or use self.run_worker(file_tree.reload_preserving_state(), name="tree-reload")
       - Override on_unmount or action_quit cleanup: call self._file_watcher.stop() before exit
    5. Write tests in tests/test_file_watcher.py:
       - test_file_watcher_service_init: instantiate with a tmp path, verify attributes
       - test_file_system_changed_message: create message with changes set, verify .changes attribute
       - test_tree_reload_on_change: create app with tmp dir, add a file, verify tree reflects the new file (may need to mock or use short timeout)
       - Note: The watchfiles awatch integration is difficult to test in unit tests due to async timing. Focus on:
         - Testing the FileSystemChanged message structure
         - Testing that FileTreePanel.reload_preserving_state calls tree.reload()
         - Testing the app handler wiring (on_file_system_changed exists and calls the right method)
  </action>
  <verify>
    <automated>cd /Users/yaroslavpankovych/nano-claude && .venv/bin/python -m pytest tests/test_file_watcher.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class FileWatcherService" nano_claude/services/file_watcher.py
    - grep -q "class FileSystemChanged" nano_claude/services/file_watcher.py
    - grep -q "awatch" nano_claude/services/file_watcher.py
    - grep -q "reload_preserving_state" nano_claude/panels/file_tree.py
    - grep -q "FileWatcherService" nano_claude/app.py
    - grep -q "on_file_system_changed" nano_claude/app.py
    - grep -q "file-watcher" nano_claude/app.py
    - ! grep -q "on_file_system_changed" nano_claude/panels/file_tree.py
  </acceptance_criteria>
  <done>FileWatcherService runs watchfiles awatch() in a background worker with 800ms debounce. When files change externally, FileSystemChanged message is posted. App.on_file_system_changed (sole handler -- app owns coordination) triggers tree reload via file_tree.reload_preserving_state(). Expanded directories are preserved across reloads. Watcher stops cleanly on app exit. All watcher tests pass.</done>
</task>

</tasks>

<verification>
1. Run full test suite: `cd /Users/yaroslavpankovych/nano-claude && .venv/bin/python -m pytest tests/ -x -q`
2. Verify search overlay exists: `grep "class SearchOverlay" nano_claude/widgets/search_overlay.py`
3. Verify SearchableTextArea exists: `grep "class SearchableTextArea" nano_claude/widgets/searchable_text_area.py`
4. Verify multi-match highlighting mechanism: `grep "render_line" nano_claude/widgets/searchable_text_area.py`
5. Verify watcher service exists: `grep "class FileWatcherService" nano_claude/services/file_watcher.py`
6. Verify Ctrl+F wired: `grep "ctrl+f" nano_claude/app.py`
7. Verify tree reload preserves state: `grep "reload_preserving_state" nano_claude/panels/file_tree.py`
8. Verify app owns FileSystemChanged handling: `grep "on_file_system_changed" nano_claude/app.py`
9. Verify FileTreePanel does NOT own FileSystemChanged: `! grep "on_file_system_changed" nano_claude/panels/file_tree.py`
</verification>

<success_criteria>
- Ctrl+F opens a search bar overlaying the top of the editor panel
- Typing a query finds matches (case-insensitive plain text search)
- All matches highlighted simultaneously: non-current matches in muted background, current match in bright yellow/distinct color (per user decision)
- Enter/Ctrl+G navigates to next match, Shift+Enter/Ctrl+Shift+G to previous
- Match count displayed as "current/total"
- Escape closes search and clears ALL highlights (both current and non-current)
- When files are added/removed externally (e.g., touch newfile.txt), tree auto-refreshes
- Expanded directories remain expanded after tree refresh
- File watcher uses 800ms debounce to batch rapid changes
- App.py is the sole owner of on_file_system_changed handler (not FileTreePanel)
- All existing tests pass (no regression)
- New search and watcher tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-tree-and-code-editor/02-03-SUMMARY.md`
</output>
